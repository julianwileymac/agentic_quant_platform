# Bot Configs Moved

Bot templates now live under [`../../aqp_bots/templates`](../../aqp_bots/templates).

The runtime registry still supports this directory as a compatibility
fallback, but new bot specs should be added to `aqp_bots/templates/trading/`
or `aqp_bots/templates/research/`.

