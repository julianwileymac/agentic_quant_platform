# Allowlist for `scripts/ci/check_mcp_audience.py` (Rule 49).
# Format: one repo-relative POSIX path per line. Lines starting with # are
# comments. Every entry MUST carry a Phase-2 removal deadline.
#
# Currently empty: Rule 49 is being enforced from green. Add entries here
# with a `TODO(phase-2, YYYY-MM-DD)` deadline if a temporary exception is
# required (e.g. an internal-only stdio bridge with no token surface).
