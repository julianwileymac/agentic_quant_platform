apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../tower-dev
  - ../../edge/cloudflared-aqp-green

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/environment: tower-green
      aqp.internal/cutover-lane: green

patches:
  - path: patches/ingress-green-hosts.yaml
  - path: patches/aqp-config-green.yaml
  - path: patches/aqp-admin-config-green.yaml
