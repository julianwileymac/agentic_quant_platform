apiVersion: apps/v1
kind: Deployment
metadata:
  name: aqp-cp
  namespace: aqp-admin
  labels:
    app: aqp-cp
    app.kubernetes.io/name: aqp-cp
    app.kubernetes.io/part-of: aqp
    app.kubernetes.io/component: control-plane
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: aqp-cp
  template:
    metadata:
      labels:
        app: aqp-cp
        app.kubernetes.io/name: aqp-cp
        app.kubernetes.io/part-of: aqp
        app.kubernetes.io/component: control-plane
    spec:
      serviceAccountName: aqp-cp
      # The control plane talks to the K8s API via the kubernetes
      # provider — it's the only AQP pod that needs the projected
      # ServiceAccount token mounted.
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: control-plane
          image: ghcr.io/julianwiley/aqp-control-plane:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: aqp-config
            - secretRef:
                name: aqp-secrets
          env:
            - name: AQP_CP_PROVIDER
              value: "kubernetes"
            - name: AQP_CP_KUBE_NAMESPACE_DEFAULT
              value: "aqp"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
          # Phase 4b control-plane maturation: split into Kubernetes-
          # native /livez (no deps) and /readyz (provider reachability).
          # /manage/health remains as a richer diagnostic for dashboards.
          readinessProbe:
            httpGet:
              path: /manage/readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /manage/livez
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /manage/readyz
              port: http
            periodSeconds: 5
            failureThreshold: 60
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: cache
              mountPath: /home/aqp/.cache
      volumes:
        - name: tmp
          emptyDir: {}
        - name: cache
          emptyDir: {}
      # Phase 4c control-plane maturation: schedule on either arm64
      # (rpi_kubernetes RPi5 edge cluster) or amd64 (standard cloud
      # nodes). Image must be multi-arch.
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 50
              preference:
                matchExpressions:
                  - key: kubernetes.io/arch
                    operator: In
                    values: ["arm64", "amd64"]
