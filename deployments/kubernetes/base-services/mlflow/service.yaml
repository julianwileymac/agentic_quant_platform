apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: aqp-mlops
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: mlflow
---
apiVersion: v1
kind: Service
metadata:
  name: mlflow-external
  namespace: aqp-mlops
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: mlflow
