apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/environment: dev

images:
  - name: docker.io/julian0215/aqp-client
    newTag: dev
  - name: docker.io/julian0215/aqp-api
    newTag: dev
  - name: docker.io/julian0215/aqp-worker
    newTag: dev
  - name: docker.io/julian0215/aqp-control-plane
    newTag: dev

# Smaller resource footprint + lower replica count for dev.
patches:
  - target:
      kind: Deployment
      name: aqp-client
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
  - target:
      kind: Deployment
      name: aqp-core
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
  - target:
      kind: Deployment
      name: aqp-worker
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
  - target:
      kind: Deployment
      name: aqp-cp
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
  # Drop PDBs to minAvailable=0 so dev can roll cleanly with replicas=1.
  - target:
      kind: PodDisruptionBudget
    patch: |-
      - op: replace
        path: /spec/minAvailable
        value: 0
  # Tighten HPA min/max for dev — don't over-provision.
  - target:
      kind: HorizontalPodAutoscaler
    patch: |-
      - op: replace
        path: /spec/minReplicas
        value: 1
      - op: replace
        path: /spec/maxReplicas
        value: 3
