apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Phase 3 §6.4 — aqp-edge (Envoy cell router).

resources:
  - deployment.yaml

images:
  - name: ghcr.io/julianwiley/aqp-edge
    newTag: dev
