apiVersion: v1
kind: Service
metadata:
  name: aqp-cp
  namespace: aqp-admin
  labels:
    app: aqp-cp
    app.kubernetes.io/part-of: aqp
spec:
  type: ClusterIP
  selector:
    app: aqp-cp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
