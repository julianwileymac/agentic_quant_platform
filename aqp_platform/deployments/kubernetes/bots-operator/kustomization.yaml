apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aqp-bots

resources:
  - service-account.yaml
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - webhook-service.yaml
  - validating-webhook-config.yaml
  - crd-installer-job.yaml

commonLabels:
  app.kubernetes.io/name: quantbot-operator
  app.kubernetes.io/part-of: quantbot-platform
