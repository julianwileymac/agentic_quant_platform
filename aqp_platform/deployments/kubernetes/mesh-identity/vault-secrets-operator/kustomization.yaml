apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Phase 4 §7.6 — vault-secrets-operator + sample VaultStaticSecret.
resources:
  - namespace.yaml
  - operator.yaml
  - sample-vault-static-secret.yaml
