# frontend

Status: compatibility stub.

The active Vite client has moved to [`../aqp_client`](../aqp_client). Use:

```bash
pnpm --dir aqp_client dev
pnpm --dir aqp_client typecheck
pnpm --dir aqp_client test
pnpm --dir aqp_client build
```

This directory is retained temporarily for local ignored build/cache files
such as `dist/` and `node_modules/`. Do not add active source here.

